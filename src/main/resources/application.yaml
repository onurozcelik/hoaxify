spring:
  datasource:
    # Cancels auto generated db name
    generate-unique-name: false 
    # url: jdbc:h2:./devdb
    # username: sa
    # password:
  # jpa:
    # hibernate:
      # ddl-auto-update: update
      # show-sql: true

